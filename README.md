# dugong

Dugong monorepo.

## Structure

- `apps/api` - public backend API.
- `apps/nautilus-server` - Nautilus server / enclave-facing service.
- `apps/worker` - background workers, pollers, indexers, and queue consumers.
- `apps/web` - frontend application.
- `contracts/move` - Move contracts.
